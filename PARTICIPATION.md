---
title: 참여 안내 (Participation Guide)
author: Kwangmin Kim
date: 2026-05-07
tags: [participation, roles, contribution, governance]
---

# 참여 안내

이 archive 는 **발표마다 한 PR 씩 자라는 공동 자료** 다. 처음부터 완벽하게 만들지 않는다. 비어 있는 섹션은 "여기는 내가 채워도 되는 자리" 라는 신호다.

주된 대상은 AI 엔지니어링이 익숙하지 않은 팀원이다.

## 1. 한 줄 철학

> 빈 자리보다 짧은 메모가 낫다. 정확하지 않아도 commit 하면 다음 사람이 고친다.

**고정 운영자는 없다.** 매 회 발표자가 자기 회분 운영을 맡고, 다음 발표자가 정해지면 운영권도 같이 넘어간다. 궁극적으로 모든 참여자가 한 회씩 운영자가 된다.

## 2. 누가 무엇을 하는가

archive 운영은 두 곳으로 분산된다.

### 2.1 현재 회 발표자가 하는 일 (1 회분 한정)

| 시점 | 일 |
|------|---|
| 발표 전 | 자기 발표 자료를 `topics/{토픽}/{slug}.md` 에 작성 |
| 발표 직후 | 자기 발표 글 frontmatter 에 `자료 링크` 추가, 하단에 "Q&A" 마크다운 헤더 섹션 추가 |
| 발표 끝 5 분 | 다음 회 발표자 합의 → 자기 발표 글 끝에 "다음 회: ..." 한 줄 |

발표자의 운영 권한은 **그 회 한정**이다. 다음 발표자가 정해지면 그 사람으로 넘어간다.

### 2.2 누구나 할 수 있는 일 (상시)

`§3 참여자` 의 Lv1~Lv4 가 여기 해당한다. 누구든 다음을 할 수 있다.

- 모든 폴더·파일에 PR (PDF 추가, 글 작성, 가이드 수정)
- 다른 사람 PR 리뷰 (의무 아님 — 가능한 사람이 함)
- 새 토픽 폴더 신설 (PR 로 제안)
- 토픽 README · 발표 글 · 가이드 등 공용 문서 수정 PR

---

## 3. 참여자가 할 수 있는 일

난이도순으로 정리했다. **Lv1 부터 시작하면 된다.** 갑자기 Lv4 부터 할 필요 없다.

### 3.1 Lv1 — 자기 도메인의 PDF 던지기 (1 분, AI 지식 0 필요)

가장 쉬운 첫 PR. 자기가 평소 읽던 논문·교재·white paper PDF 를 `references/pdfs/` 에 추가하고 push 한다.

```bash
git checkout -b add-pdf-{slug}
cp ~/Downloads/your-paper.pdf references/pdfs/
git add references/pdfs/your-paper.pdf
git commit -m "references: add {제목}"
git push -u origin add-pdf-{slug}
```

추후 누군가 `/convert-pdf` 스킬을 호출하면 `references/md/` 에 자동 변환된 markdown 이 생성된다 (첫 실행 시 marker 의존성도 자동 설치됨).

### 3.2 Lv2 — 발표 글 Q&A 보강 (5 분)

`topics/{토픽}/...md` 발표 글 하단의 Q&A 섹션을 보고, "이 질문 답을 알 것 같다" 는 미답 항목에 1 단락 추가한다. 발표 당일 답을 못 한 질문이 있을 때 후속으로 채우는 자리다.

### 3.3 Lv3 — 토픽에 짧은 메모 작성 (15 분)

자기 도메인에 AI 를 적용해 본 경험·실패담·시도 중인 아이디어를 `topics/{원하는 토픽}/{slug}.md` 에 메모한다. 형식은 자유 — 다른 발표 글 (예: `topics/engineering/0-skill-based-prompt-engineering.md`) 을 참고하면 된다.

분량 기준은 없다. 1 단락도 환영한다. 적용 결과가 실패였어도 OK — 그 자체가 다음 사람에게 정보다.

### 3.4 Lv4 — 발표 자원 (1 회, 30~60 분 준비)

자기 도메인 사례 또는 자기가 학습 중인 AI 주제로 30~50 분 세미나 발표. 슬라이드는 자유 형식 (PPT, Keynote, Marp, Quarto 무관). 발표 자료는 `topics/{토픽}/{slug}.md` 에 작성하고, 발표 후 그 글 frontmatter 에 `slides`·`recording` 링크와 하단 "Q&A" 섹션을 PR 로 추가한다.

**Lv4 발표자는 그 회 운영자다** — `§2.1` 의 3 가지 일 (글 작성·발표 직후 갱신·다음 발표자 합의) 을 자기 회 한정으로 맡는다.

---

## 4. 처음 PR 하는 법 (GitHub 처음이어도 OK)

git CLI 가 익숙하지 않다면 GitHub 웹 UI 만으로도 PR 이 가능하다.

1. GitHub 에서 추가·수정하고 싶은 파일 위치로 이동
2. 우상단 연필 아이콘 (Edit this file) 또는 Add file → Create new file
3. 내용 작성 후 페이지 하단 "Propose changes" → 새 브랜치 자동 생성
4. PR 생성 화면에서 한 줄 설명 (예: "topics/agents: tool use 메모 추가") 후 Create pull request
5. 동료 1 명 리뷰 후 merge. 사소한 PR 은 self-merge 도 허용한다 (PDF 추가, 오타 수정, Q&A 한 단락 추가 등).

오타·형식 문제만 있는 PR 은 **거절하지 않는다**. 다른 참여자가 직접 수정 후 merge 하거나 그대로 merge 한다.

---

## 5. 분쟁 해결 룰

PR 충돌·새 토픽 명명 다툼·가이드 변경 이견이 생기면:

1. PR 코멘트로 의견을 남긴다 (24 시간 대기)
2. 합의 안 되면 **다음 발표 자리에서 5 분 안에 합의**
3. 그래도 안 되면 그 회 발표자가 임시 결정권을 가진다 (그 회 한정)

가이드 변경 (`CLAUDE.md`·`GUIDE.md`·`PARTICIPATION.md` 등) 은 PR + 다른 참여자 1 명 동의 후 merge.

---

## 6. 지켜야 할 것 (최소 가드레일)

| 항목 | 이유 |
|------|------|
| `.env` · API key · password 같은 secrets commit 금지 | 보안 사고 |
| `references/md/` 의 자동 변환 결과는 직접 수정하지 않는다 | 다음 변환 때 덮어써짐. 보강이 필요하면 `topics/` 에 별도 글로 |
| 다른 사람 글·이름 임의 변경 금지 | 의도가 다를 수 있음. 변경이 필요하면 PR 로 제안 |
| 본문 한다 체 (`~한다/~이다/~된다`) 사용 | archive 일관성 — 자세한 규칙은 `guides/GUIDE_CORE.md` |
| 마크다운 본문에 이모지 금지 | 위와 동일 |

---

## 7. 자주 묻는 질문

**Q. 운영자가 누구인가요?**
**고정 운영자는 없다.** 매 회 발표자가 자기 회분 운영을 맡는다 (`§2.1`). 그 외 모든 참여자는 동등하다.

**Q. AI 를 잘 모르는데 기여해도 되나요?**
가능하다. 사실 그게 이 archive 의 핵심 청중이다. Lv1 (PDF 던지기) 또는 Lv3 (자기 도메인 메모) 부터 시작하면 된다. AI 엔지니어가 아닌 사람의 시선이 토픽을 더 풍부하게 만든다.

**Q. 내 도메인이 토픽 카테고리에 없어요.**
`topics/` 아래 새 폴더 (예: `bio`, `research`, `applications`) 를 만들어 PR 하거나, 다음 발표 자리에서 새 토픽을 제안한다. 새 토픽 신설은 환영한다.

**Q. 내용이 짧아도 되나요?**
1 단락·1 줄도 OK. **빈 자리보다 짧은 메모가 낫다.** 다음 사람이 보강하면 된다.

**Q. 정확하지 않은 내용을 써도 되나요?**
문제없다. 본문 끝에 `(확인 필요)` 또는 `(근거 부족)` 만 붙이면 된다. 다른 참여자가 보강·수정한다.

**Q. 한국어로 써야 하나요?**
한국어를 기본으로 하지만 영어도 무방하다. 인용 원문은 그대로 두고 한국어 해설을 붙이는 형식을 권장한다.

**Q. 슬라이드를 발표 후 어디에 두나요?**
PDF 로 export 해 `references/pdfs/` 에 두거나, 외부 링크(Notion·Google Slides)를 발표 자료 글(`topics/{토픽}/{slug}.md`) 의 frontmatter `slides` 필드에 적는다. 둘 다 OK.

---

## 8. 한 줄 요약

고정 운영자는 없다. **발표자가 한 회분 운영하고, 모두가 각자 한 칸씩 채운다.** 빈 칸은 잘못이 아니라 다음 차례의 자리다.
